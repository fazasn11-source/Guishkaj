@@
-      - name: Set up Gradle (if no wrapper)
-        uses: gradle/gradle-build-action@v3
-        with:
-          gradle-version: '8.6'
+      - name: Set up Gradle (if no wrapper)
+        uses: gradle/gradle-build-action@v3
+        with:
+          gradle-version: '8.12'
